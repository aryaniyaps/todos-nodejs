rules:
  - id: software-factory-anti-slop
    message: Focused tests must not narrow qualification evidence.
    severity: ERROR
    languages: [javascript, typescript]
    pattern-either:
      - pattern: test.only(...)
      - pattern: it.only(...)
      - pattern: describe.only(...)
      - pattern: fit(...)
*** Add File: /tmp/todos-nodejs-setup-2/.dagger/semgrep/software-factory-anti-slop.test.ts
test.only("focused", () => true)
it.only("focused", () => true)
describe.only("focused", () => true)
fit("focused", () => true)
